This is the future home of the canonical Rook Environment Repository.

Deployment note: `environments.the-rooks-nest.com` points at `http://localhost:18082`, so the production service should listen on port `18082`.
