name: Debug SSH deploy access

on:
  workflow_dispatch:

jobs:
  debug-ssh:
    runs-on: ubuntu-latest
    env:
      DEPLOY_HOST: ${{ secrets.ROOK_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.ROOK_DEPLOY_USER }}
      DEPLOY_PORT: ${{ secrets.ROOK_DEPLOY_PORT }}
      CLOUDFLARE_ACCESS_CLIENT_ID: ${{ secrets.ROOK_CLOUDFLARE_ACCESS_CLIENT_ID }}
      CLOUDFLARE_ACCESS_CLIENT_SECRET: ${{ secrets.ROOK_CLOUDFLARE_ACCESS_CLIENT_SECRET }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Write deploy SSH key
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.ROOK_DEPLOY_SSH_KEY }}" > ~/.ssh/rook_deploy
          chmod 600 ~/.ssh/rook_deploy

      - name: Validate SSH key
        run: |
          ssh-keygen -y -f ~/.ssh/rook_deploy > /tmp/rook_deploy.pub
          ssh-keygen -lf /tmp/rook_deploy.pub

      - name: Install cloudflared
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared-linux-amd64.deb
          cloudflared --version

      - name: Configure SSH for Cloudflare Tunnel
        run: |
          cat > ~/.ssh/config <<EOF
          Host production-mac
            HostName ${DEPLOY_HOST}
            User ${DEPLOY_USER}
            Port ${DEPLOY_PORT:-22}
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            IdentityFile ~/.ssh/rook_deploy
            IdentitiesOnly yes
            ServerAliveInterval 30
            ProxyCommand env CLOUDFLARE_ACCESS_CLIENT_ID=${CLOUDFLARE_ACCESS_CLIENT_ID} CLOUDFLARE_ACCESS_CLIENT_SECRET=${CLOUDFLARE_ACCESS_CLIENT_SECRET} cloudflared access tcp --hostname %h

          Host bustedscreen-hardcoded
            HostName bustedscreen.arcturus-labs.com
            User johnberryman
            Port 22
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            IdentityFile ~/.ssh/rook_deploy
            IdentitiesOnly yes
            ServerAliveInterval 30
            ProxyCommand env CLOUDFLARE_ACCESS_CLIENT_ID=${CLOUDFLARE_ACCESS_CLIENT_ID} CLOUDFLARE_ACCESS_CLIENT_SECRET=${CLOUDFLARE_ACCESS_CLIENT_SECRET} cloudflared access tcp --hostname %h
          EOF
          chmod 600 ~/.ssh/config
          sed 's/=.*/=***MASKED***/g' ~/.ssh/config || true

      - name: Test SSH connectivity using repo secrets
        run: |
          ssh -vvv production-mac 'echo connected && whoami && hostname && pwd'

      - name: Test SSH connectivity using hardcoded known-good host
        run: |
          ssh -vvv bustedscreen-hardcoded 'echo connected && whoami && hostname && pwd'
